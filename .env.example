# === MCP Server Environment Variables ===

# Cloudflare MCP (@cloudflare/mcp-server-cloudflare)
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token-here
# Find your Account ID at: https://dash.cloudflare.com → pick an account → overview
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id-here

# GitHub MCP (@modelcontextprotocol/server-github)
# Create a Personal Access Token at: https://github.com/settings/tokens
# Required scopes: repo, workflow, read:org, read:user
GITHUB_PERSONAL_ACCESS_TOKEN=your-github-personal-access-token-here

# === Team Canada ICC App ===

# Web3Forms API Key
# Get your free API key at: https://web3forms.com/#start
WEB3FORMS_API_KEY=your-web3forms-api-key-here

# Cloudflare Turnstile Secret
# Created via `npx wrangler turnstile widget create` or from the Cloudflare dashboard
# For local development only; on Pages, use `wrangler pages secret put TURNSTILE_SECRET`
TURNSTILE_SECRET=your-turnstile-secret-here

# Resend API Key
# Create one at: https://resend.com/api-keys (the sending domain must be verified first)
# For local development only; on Pages, use `wrangler pages secret put RESEND_API_KEY`
RESEND_API_KEY=your-resend-api-key-here
# Optional overrides — defaults live in functions/api/inquiry.ts
# RESEND_FROM=Team Canada ICC <partnerships@send.teamcanadaicc.ca>
# RESEND_REPLY_TO=partnerships@teamcanadaicc.ca

# Origin used to build the partnership-package download link that goes out by
# email. Set this in production so the link can never be steered by a forged
# Host header. Preview deploys (*.pages.dev) and local dev self-reference
# without it; anything else falls back to the production origin.
# SITE_ORIGIN=https://teamcanadaicc.ca
